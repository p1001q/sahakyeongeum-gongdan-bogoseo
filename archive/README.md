# 보관 자료 안내

- 정리일: 2026-08-31
- 목적: 현재 연구 흐름에서 사용하지 않는 과거 산출물을 삭제하지 않고 활성 작업 경로와 분리한다.

## 보관 원칙

- `legacy_report_20260831/`: 공공데이터 186개 전수검토와 최종 구조 확정 전에 제작된 DOCX·PDF 및 당시 수치검증표다. **현재 제출 후보가 아니며 내용 근거로 직접 사용하지 않는다.**
- `forms_application_20260827/`: 가명정보 신청 관련 원본 양식과 당시 작성본이다. 서명 이미지 등 개인정보가 포함돼 있어 **`.gitignore`로 제외돼 있다. 로컬에만 존재하며 GitHub 원격 저장소(퍼블릭)에는 올라가지 않는다.** 외부 공유 금지.
- `legacy_scripts/`: 위 과거 보고서와 신청서를 만들 때 사용한 제작 스크립트다. 사용자 실명·전화번호·이메일·주소·생년월일이 하드코딩된 `legacy_scripts/build_forms.py`가 포함돼 있어 **`.gitignore`로 제외돼 있다. 로컬에만 존재하며 GitHub 원격 저장소(퍼블릭)에는 올라가지 않는다.** 현재 분석 파이프라인에는 포함하지 않는다.
- `data_verification_v1_20260831/`: 팀원의 186개 데이터 2차 신뢰성 검증으로 `docs/15`와 `data/catalog/` 186개 목록·분류요약이 갱신되기 전 원본이다. 자세한 변경 내역은 그 폴더의 `README.md` 참고.

## GitHub 연동 상태 (2026-08-31)

- 이 워크스페이스는 GitHub 퍼블릭 저장소로 연동돼 있다. `forms_application_20260827/`와 `legacy_scripts/`는 개인정보를 포함하므로 위 gitignore 규칙에 따라 원격 저장소에는 절대 올라가지 않는다.
- 새 파일을 이 두 폴더 아래에 추가하더라도 자동으로 커밋 대상에서 제외되지만, 다른 위치에 개인정보를 새로 저장할 때는 커밋 전 반드시 재확인한다.

현재 연구의 진입점은 루트의 `START_HERE.md`, 문서 지도는 `docs/00_문서_인덱스_및_현재_기준.md`를 따른다.
