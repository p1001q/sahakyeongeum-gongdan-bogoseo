# Workspace continuity instructions

이 폴더는 사립학교교직원연금공단 공공데이터 활용 연구 프로젝트의 지속 작업공간이다.

## 세션 시작 시

1. `START_HERE.md`를 가장 먼저 읽어 최신 방향과 즉시 행동을 파악한다.
2. `docs/00_문서_인덱스_및_현재_기준.md`를 읽어 문서별 최신·과거 상태를 구분한다.
3. `USER_CONTEXT.md`와 `PROJECT_CONTEXT.md` 맨 위의 `현재 확정 기준`을 읽는다.
4. 최종 구조와 데이터 판단은 `docs/16_최종_보고서_목차_및_논리구조_픽스_2026-08-31.md`, `docs/15_사학연금공단_공공데이터_186개_전수검토_2026-08-31.md`를 우선한다.
5. 담당자 회신·평가·양식 근거가 필요하면 `docs/10_강석_담당자_회신_및_프로젝트_방향_2026-08-29.md`, `docs/13_공단_제출보고서_사고흐름_및_양식가이드_2026-08-30.md`, `docs/01_추진계획안_전문분석.md`를 읽는다.
6. `docs/00`에서 과거 참고로 분류한 문서는 의사결정 이력으로만 사용하고 현재 실행 지시로 사용하지 않는다.
7. 새 파일이나 데이터가 추가되었는지 확인하고, 기존 맥락과 충돌하면 사용자에게 충돌 내용을 먼저 알린다.

## 작업 중·종료 시

- 중요한 결정, 새 사실, 데이터 입수 상태, 분석 결과, 미해결 질문, 다음 행동이 생긴 세션에는 `PROJECT_CONTEXT.md`를 갱신한다.
- 사용자 역할·선호·팀 상황이 바뀌면 `USER_CONTEXT.md`를 갱신한다.
- 사실, 가설, 제안을 구분한다. 확인되지 않은 내용을 확정 사실처럼 메모하지 않는다.
- 출처가 있는 사실에는 URL·문서명·확인일을 남긴다.
- 변경일은 Asia/Seoul 기준 `YYYY-MM-DD`로 기록한다.
- 원본 데이터와 원본 문서는 덮어쓰지 않는다.

## 데이터 보호

- 가명데이터 원본은 저장 위치와 반출 조건을 먼저 확인한 뒤 다룬다.
- 개인식별 가능성이 있는 원시 행, 정확한 생년월일·기관명·주소·연락처를 문서나 결과물에 불필요하게 복제하지 않는다.
- 보고서·PPT·대시보드에는 소수 셀 억제, 범주화 등 재식별 위험 통제를 적용한다.
- 외부 이메일 발송, 포털 신청, 파일 업로드는 사용자의 명시적 요청 없이 수행하지 않는다.

## 기본 작업 언어와 방식

- 기본 언어는 한국어다.
- 사용자는 공공데이터 연구가 처음이므로 결론과 이유를 쉬운 말로 설명하되, 분석은 재현 가능하게 만든다.
- 결과는 공단의 실제 행동으로 이어지도록 `문제 -> 근거 -> 해석 -> 제안 -> 성과지표` 구조를 우선한다.
- 현재 핵심 산출물은 전문 보고서 제작 AI용 장별 구조·본문 재료·시각자료·팩트시트와 재현 코드/데이터 사전이다. 사용자가 다시 지시하기 전에는 DOCX·PDF·PPT·앱·대시보드를 직접 제작하지 않는다.
